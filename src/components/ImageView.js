import React from "react";
import { Text, View, Image } from "react-native";

const ImageView = ({ title, image }) => {
  return (
    <View>
      <Image source={forest.jpg} />
    </View>
  );
};

export default ImageView;
